-- Este archivo configura el plugin `dressing.nvim`, que permite cambiar rápidamente entre esquemas de colores en Neovim.

return {
  "stevearc/dressing.nvim", -- Plugin para cambiar rápidamente entre esquemas de colores en Neovim
  event = "VeryLazy", -- Evento en el que se activa el plugin (configurado como "MuyVago")
}

